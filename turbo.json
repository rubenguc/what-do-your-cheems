{
  "globalEnv": [".env"],
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": [
        "^build"
      ],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "dev": {
      "cache": false,
      "dotEnv": [".env"]
    },
    "seed": {
      "cache": false
    }
  }
}